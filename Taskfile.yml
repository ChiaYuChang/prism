version: "3"

# 自動載入環境變數
dotenv: [".env"]

vars:
  DEPLOY_DIR: deployments
  BUILD_DIR: bin
  CMD_DIR: cmd
  DB_DIR: db

  POSTGRES_APP_PASSWORD: "$(cat {{.POSTGRES_APP_PASSWORD_FILE}})"
  POSTGRES_ADMIN_PASSWORD: "$(cat {{.POSTGRES_ADMIN_PASSWORD_FILE}})"
  POSTGRES_URL: "postgres://{{.POSTGRES_ADMIN_USER}}:{{.POSTGRES_ADMIN_PASSWORD}}@localhost:{{.POSTGRES_PORT}}/{{.POSTGRES_APP_DB}}?sslmode=disable"

  COMPOSE_SOURCES: >-
    -f {{.DEPLOY_DIR}}/docker-compose.yaml 
    -f {{.DEPLOY_DIR}}/docker-compose.dev.yaml 
    -f {{.DEPLOY_DIR}}/docker-compose.obs.yaml 
    -f {{.DEPLOY_DIR}}/docker-compose.ai.yaml
  COMPOSE_MERGED: .docker-compose.merged.yaml

tasks:
  default:
    desc: list all available tasks
    cmds:
      - task --list

  # ----------------------------------------------------------------------------
  # Agent
  # ----------------------------------------------------------------------------
  codex:
    cmds:
      - codex resume {{.CODEX_SESSION_ID}}
  claude:
    cmds:
      - claude --resume {{.CLAUDE_SESSION_ID}}
  gemini:
    cmds:
      - gemini --resume {{.GEMINI_SESSION_ID}}

  # ----------------------------------------------------------------------------
  # Service Management (Docker Compose)
  # ----------------------------------------------------------------------------
  compose:bake:
    desc: combine all docker-compose files into one
    cmds:
      - docker compose {{.COMPOSE_SOURCES}} config > {{.COMPOSE_MERGED}}
    sources:
      - "{{.DEPLOY_DIR}}/docker-compose*.yaml"
      - .env
    generates:
      - .docker-compose.merged.yaml

  compose:up:
    desc: start all services
    deps: [compose:bake]
    cmds:
      - docker compose -f {{.COMPOSE_MERGED}} up -d

  compose:down:
    desc: stop and remove all services
    cmds:
      - docker compose -f {{.COMPOSE_MERGED}} down

  compose:clean:
    desc: stop and remove all services and volumes
    cmds:
      - docker compose -f {{.COMPOSE_MERGED}} down -v --remove-orphans

  compose:logs:
    desc: list all service logs
    deps: [compose:bake]
    cmds:
      - docker compose -f {{.COMPOSE_MERGED}} logs -f

  # ----------------------------------------------------------------------------
  # Development & Code Generation
  # ----------------------------------------------------------------------------
  init:
    desc: initialize development environment
    deps: [tidy, gen]
    cmds:
      - echo "Development environment initialized"

  db-dump:
    desc: Dump the database to a file
    cmds:
      - pg_dump "{{.POSTGRES_URL}}" -f {{.DB_DIR}}/schema.sql --schema-only
      - grep -v '\\unrestrict ' {{.DB_DIR}}/schema.sql | grep -v '\\restrict' > {{.DB_DIR}}/schema.sql.tmp && mv {{.DB_DIR}}/schema.sql.tmp {{.DB_DIR}}/schema.sql

  gen:
    desc: generate code
    cmds:
      - task: db-dump
      - task: sqlc
      - task: mocks

  sqlc:
    desc: generate code using sqlc
    cmds:
      - sqlc generate -f sqlc.json

  mocks:
    desc: generate mocks using mockery v3
    cmds:
      - go tool mockery

  tidy:
    desc: tidy go dependencies
    cmds:
      - go mod tidy
      - go mod verify

  # ----------------------------------------------------------------------------
  # Database Migration
  # ----------------------------------------------------------------------------

  migrate:up:
    desc: run database migration
    cmds:
      - migrate -path {{.DB_DIR}}/migrations -database "{{.POSTGRES_URL}}" up

  migrate:down:
    desc: rollback last migration
    cmds:
      - migrate -path {{.DB_DIR}}/migrations -database "{{.POSTGRES_URL}}" down 1

  migrate:clean:
    desc: rollback last migration
    cmds:
      - migrate -path {{.DB_DIR}}/migrations -database "{{.POSTGRES_URL}}" down

  migrate:force:
    desc: Force a migration version (use with caution!)
    vars:
      VERSION: "{{.VERSION}}"
    cmds:
      - migrate -path {{.DB_DIR}}/migrations -database "{{.POSTGRES_URL}}" force {{.VERSION}}

  migrate:create:
    desc: create new migration file (NAME=xxx)
    cmds:
      - migrate create -ext sql -dir {{.DB_DIR}}/migrations -seq {{.NAME}}
    preconditions:
      - sh: 'test -n "{{.NAME}}"'
        msg: "Please provide a name for the migration file, e.g. task migrate:create NAME=add_users_table"

  # ----------------------------------------------------------------------------
  # Build & Test
  # ----------------------------------------------------------------------------
  test:
    desc: run unit tests
    cmds:
      - go test -v -short -cover ./...

  test:scheduler:
    desc: run scheduler in test mode (gochannel)
    cmds:
      - go run ./cmd/scheduler --messenger-type gochannel --interval 1m --host {{.POSTGRES_HOST}} --port {{.POSTGRES_PORT}} --user {{.POSTGRES_APP_USER}} --password "{{.POSTGRES_APP_PASSWORD}}" --db {{.POSTGRES_APP_DB}}

  test:discovery:
    desc: run discovery worker in test mode (gochannel)
    cmds:
      - go run ./cmd/worker/discovery --messenger-type gochannel --host {{.POSTGRES_HOST}} --port {{.POSTGRES_PORT}} --user {{.POSTGRES_APP_USER}} --password "{{.POSTGRES_APP_PASSWORD}}" --db {{.POSTGRES_APP_DB}}

  build:all:
    desc: build all microservice binaries
    cmds:
      - task: build:scheduler
      - task: build:discovery
      - task: build:pipeline

  build:scheduler:
    cmds: ["go build -o {{.BUILD_DIR}}/scheduler ./{{.CMD_DIR}}/scheduler/*.go"]

  build:discovery:
    cmds:
      [
        "go build -o {{.BUILD_DIR}}/discovery ./{{.CMD_DIR}}/worker/discovery/main.go",
      ]

  build:pipeline:
    cmds: ["go build -o {{.BUILD_DIR}}/pipeline ./cmd/pipeline-worker/main.go"]

  # ----------------------------------------------------------------------------
  # Run Services (Local Development)
  # ----------------------------------------------------------------------------
  run:scheduler:
    desc: run scheduler on host
    cmds:
      - go run cmd/scheduler/main.go

  run:discovery:
    desc: run discovery worker on host
    cmds:
      - go run cmd/worker/discovery/main.go

  run:pipeline:
    desc: run pipeline worker on host
    cmds:
      - go run cmd/pipeline-worker/main.go
