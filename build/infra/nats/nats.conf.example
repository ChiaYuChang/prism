# NATS Server Configuration

# General
port: 4222
http_port: 8222
server_name: prism-nats

# JetStream
jetstream {
    store_dir: /data/jetstream
}

# Authorization
authorization {
    # Replace with your actual authentication token
    # token: replace-with-your-nats-auth-token
}
